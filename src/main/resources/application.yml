server:
  port: 1234
spring:
  application:
    name: prodsel
  datasource:
    url: jdbc:mysql://localhost:3306/prodsell?createDatabaseIfNotExist=true
    username: root
    password: T.12U!Zx
  sql:
    init:
      mode: ALWAYS
      platform: mysql
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
    properties:
      hibernate:
        jdbc:
          batch_size: 20
    show-sql: true
